# Write a loop that checks if each value is a positive number (i.e. a digit and greater than 0).
# If a value is invalid, print it.

list = ["3", "4", "a", "-1"]
invalid_list = []


class player:
    def __init__(self):
        self.player_name = []
        self.player_score = []
        
    def calc_score(self):
        